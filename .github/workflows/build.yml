name: build

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  workflow_dispatch:

# The release steps create GitHub Releases, which needs write access. The repo's
# default workflow token is read-only — that caused 403 "Resource not accessible
# by integration" and silently skipped publishing. Grant it explicitly here.
permissions:
  contents: write

# ── Third-party binaries: pinned, and checked before they are used ──────────
#
# Every one of these is downloaded over the network and then either RUN on the
# runner or BUNDLED into an installer that we go on to Authenticode-sign and
# notarise. Our signature is what tells SmartScreen and Gatekeeper the contents
# are ours, so shipping a binary we never verified means vouching for something
# we did not check.
#
# Two problems, both fixed here:
#   - `latest` / `continuous` are moving tags. Two builds of the same commit
#     could ship different ffmpeg and yt-dlp, and after the fact there is no way
#     to say which. Every reference below names an immutable release.
#   - Nothing was verified. There was no sha256sum, shasum or Get-FileHash
#     anywhere in this file. Now each download is checked before use and the job
#     fails loudly if a hash moves.
#
# yt-dlp publishes its own signed SHA2-256SUMS with each release, so that is
# what we check against — a stronger chain of custody than a hash we copied.
# linuxdeploy publishes no checksums, and BtbN's checksums.sha256 sits in the
# same release as the file it vouches for, so their hashes are recorded here;
# bump the tag and the hash together.
#
# KEEP THESE FRESH. A pinned yt-dlp goes stale and stale yt-dlp means broken
# extractors, which is the single most common cause of "this video won't
# download". Bump YTDLP_VERSION on a regular cadence — this pin is for
# reproducibility and provenance, not for standing still.
env:
  YTDLP_VERSION: '2026.08.19'
  YTDLP_SHA256_LINUX: '1fa6733c37ea6fb51c99ad8fe785e7b7e5f3246c9b980230329d4fb72ed8d4d6'
  YTDLP_SHA256_WINDOWS: '66674953fe251b89f4d08c5f0e35e0728679bd67ab3d7d05c0562af101dd3e7a'
  YTDLP_SHA256_MACOS: '0f192b7ec147ab6288885d6351d9ab67367640029b4377576ef46dd79cf7b202'
  # A release-branch build, not master: this one ships to users. ffmpeg 8.x also
  # carries the `allowed_segment_extensions` guard that stops a hostile HLS
  # playlist reading local files through a rewritten segment URI.
  # Pin a MONTH-END autobuild, the last one of its month. BtbN deletes daily
  # builds after about two weeks but keeps each month's last build for about two
  # years; a daily pin (autobuild-2026-09-08-23-15) vanished and every package
  # job failed here with a 404.
  FFMPEG_TAG: 'autobuild-2026-08-31-13-27'
  FFMPEG_ASSET: 'ffmpeg-n8.1.2-50-g1a748fe2cd-win64-gpl-8.1.zip'
  FFMPEG_SHA256: '273abb45f3f9f76c303e35ff39f5bb6c23c163ae65f6244a32b7d4a7f6cf0616'
  # Same release, same ffmpeg version, static Linux build. The .deb used to copy
  # the runner's apt /usr/bin/ffmpeg, which is dynamically linked against eight
  # libav*/libsw*/libpostproc libraries the package never shipped — see the
  # bundling step below.
  FFMPEG_ASSET_LINUX: 'ffmpeg-n8.1.2-50-g1a748fe2cd-linux64-gpl-8.1.tar.xz'
  FFMPEG_SHA256_LINUX: 'c733b4b2951e5957e15505f788b2c65a7a41b6da4b289e295852cc38079b4d2b'
  # Build-time only — these never reach a user's machine, but they DO execute on
  # a runner that holds the signing secrets.
  LINUXDEPLOY_TAG: '1-alpha-20251107-1'
  LINUXDEPLOY_SHA256: 'c20cd71e3a4e3b80c3483cef793cda3f4e990aca14014d23c544ca3ce1270b4d'
  LINUXDEPLOY_QT_TAG: '1-alpha-20250213-1'
  LINUXDEPLOY_QT_SHA256: '15106be885c1c48a021198e7e1e9a48ce9d02a86dd0a1848f00bdbf3c1c92724'

# Every job sets timeout-minutes at several times its normal run time. Without
# one GitHub waits 6 hours, and a hung step (the macOS smoke test once launched
# the whole app and never returned) held its runner for all of them.
jobs:
  # ── Linux CI ───────────────────────────────────────────────────────────────
  linux:
    name: Build + .deb package (Ubuntu)
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v7

      - name: Install toolchain
        run: |
          sudo apt-get update
          sudo apt-get install -y cmake ninja-build \
            qt6-base-dev libqt6sql6-sqlite qt6-tools-dev qt6-tools-dev-tools \
            libtorrent-rasterbar-dev \
            patchelf file \
            ffmpeg yt-dlp aria2 \
            dpkg-dev

      - name: Configure & Build
        run: |
          cmake -B build -G Ninja \
            -DCMAKE_BUILD_TYPE=Release \
            -DCMAKE_INSTALL_PREFIX=/usr
          cmake --build build

      - name: Smoke test
        timeout-minutes: 5
        run: |
          test -x build/nexa && test -x build/nexa-host && echo "Build OK"
          # Runs the binary for real: --version exits before a window or display
          # is needed, so this proves every library it links resolves.
          ./build/nexa --version

      # The package version comes from CMake's project() — the same number the
      # binary reports to the update feed — so a release bump is one edit, not
      # four. A .deb stamped 0.2.0 around a 0.2.1 binary made dpkg treat the
      # upgrade as a reinstall and the website list the wrong file.
      - name: Read project version
        run: |
          v=$(sed -n 's/^project(Nexa VERSION \([0-9][0-9.]*\).*/\1/p' CMakeLists.txt)
          test -n "$v" || { echo "could not read project version from CMakeLists.txt"; exit 1; }
          echo "NEXA_VERSION=$v" >> "$GITHUB_ENV"
          echo "Packaging version $v"

      - name: Gather FULL dependency closure (linuxdeploy + Qt plugins)
        run: |
          set -e
          # linuxdeploy copies the COMPLETE recursive library closure of nexa +
          # nexa-host (Qt6, libtorrent, boost, ssl, ICU, fontconfig, freetype,
          # harfbuzz, xcb, libpng, …) and bundles the Qt platform / sqldrivers /
          # tls / imageformats / iconengines plugins. AppImages need FUSE (absent
          # on CI), so we --appimage-extract and run the inner AppRun directly.
          # Pinned + verified: these AppImages execute on this runner.
          dl() {
            curl -sSL --fail "$1" -o "$2"
            echo "$3  $2" | sha256sum -c - || { echo "ERROR: $2 failed its checksum"; exit 1; }
            chmod +x "$2"
          }
          dl "https://github.com/linuxdeploy/linuxdeploy/releases/download/${LINUXDEPLOY_TAG}/linuxdeploy-x86_64.AppImage" \
             linuxdeploy "$LINUXDEPLOY_SHA256"
          dl "https://github.com/linuxdeploy/linuxdeploy-plugin-qt/releases/download/${LINUXDEPLOY_QT_TAG}/linuxdeploy-plugin-qt-x86_64.AppImage" \
             linuxdeploy-plugin-qt "$LINUXDEPLOY_QT_SHA256"
          ./linuxdeploy            --appimage-extract >/dev/null && mv squashfs-root ld
          ./linuxdeploy-plugin-qt  --appimage-extract >/dev/null && mv squashfs-root ldqt
          # linuxdeploy discovers its qt plugin by name on PATH
          ln -sf "$PWD/ldqt/AppRun" ld/usr/bin/linuxdeploy-plugin-qt
          export PATH="$PWD/ld/usr/bin:$PATH"
          export QMAKE="$(command -v qmake6)"
          [ -n "$QMAKE" ] || { echo "ERROR: qmake6 not found (qt6-base-dev missing?)"; exit 1; }
          command -v linuxdeploy-plugin-qt >/dev/null \
            || { echo "ERROR: linuxdeploy-plugin-qt not discoverable on PATH"; exit 1; }
          mkdir -p AppDir
          # xcb is the only platform plugin we bundle: it serves X11 AND XWayland,
          # which runs on essentially every desktop Wayland session — so a separate
          # (and fiddly-to-bundle) wayland plugin set isn't worth the surface area.
          ./ld/AppRun --appdir AppDir \
            -e build/nexa -e build/nexa-host \
            --plugin qt

          # DENY = the only libraries we DON'T bundle, because they must come from
          # the host: glibc (ABI-tied to the kernel), libstdc++/libgcc (forward-
          # compatible, safer from the host) and the GPU/GL driver stack (bundling
          # a driver lib black-screens/crashes on a different GPU). Everything else
          # gets bundled. These are declared as Depends below.
          DENY='^(ld-linux.*|libc|libm|libdl|libpthread|librt|libresolv|libnss_[a-z]+|libstdc\+\+|libgcc_s|libGL|libGLX|libGLdispatch|libOpenGL|libEGL|libdrm|libgbm|libglapi)\.so'

          # linuxdeploy honours the AppImage excludelist, which deliberately does
          # NOT bundle libfontconfig / libfreetype / libharfbuzz / libSM / libICE
          # (and the GL stack). For a TRUE offline bundle we resolve the full
          # recursive closure of both binaries AND every bundled plugin ourselves,
          # copying any non-DENY lib linuxdeploy left behind. Several passes let
          # newly-copied libs pull in their own deps until it converges.
          for pass in 1 2 3 4 5 6; do
            { find AppDir/usr/lib AppDir/usr/plugins -name '*.so*' -type f
              echo build/nexa; echo build/nexa-host; } | while IFS= read -r obj; do
              ldd "$obj" 2>/dev/null | awk '/=> \//{print $3}'
            done | sort -u | while IFS= read -r lib; do
              base="$(basename "$lib")"
              echo "$base" | grep -qE "$DENY" && continue
              [ -e "AppDir/usr/lib/$base" ] && continue
              cp -Ln "$lib" AppDir/usr/lib/ 2>/dev/null && echo "  + bundled $base"
            done
          done

          # Drop any DENY lib linuxdeploy may have bundled (so it stays host-side).
          for f in $(ls AppDir/usr/lib); do
            echo "$f" | grep -qE "$DENY" && rm -f "AppDir/usr/lib/$f"
          done || true

          echo "=== libs bundled: $(ls AppDir/usr/lib | wc -l) ===" && ls AppDir/usr/lib | sort
          echo "=== plugins bundled ===" && find AppDir/usr/plugins -type f | sort
          for d in platforms sqldrivers tls imageformats iconengines styles; do
            echo "  $d: $(find AppDir/usr/plugins/$d -name '*.so' 2>/dev/null | wc -l) plugin(s)"
          done
          # Hard requirements: the xcb platform plugin (GUI can't start without it)
          # and at least one TLS backend (every https download fails without it).
          test -f AppDir/usr/plugins/platforms/libqxcb.so
          test -n "$(find AppDir/usr/plugins/tls -name '*.so' 2>/dev/null)"

      - name: Download yt-dlp binary
        run: |
          base="https://github.com/yt-dlp/yt-dlp/releases/download/${YTDLP_VERSION}"
          curl -sSL --fail "$base/yt-dlp" -o yt-dlp
          # Check against the SUMS file from the same immutable release, then
          # against our own pin — so a swapped release asset AND a swapped sums
          # file both have to get past us.
          curl -sSL --fail "$base/SHA2-256SUMS" -o SHA2-256SUMS
          grep -E '  yt-dlp$' SHA2-256SUMS | sha256sum -c - \
            || { echo "ERROR: yt-dlp does not match upstream SHA2-256SUMS"; exit 1; }
          echo "$YTDLP_SHA256_LINUX  yt-dlp" | sha256sum -c - \
            || { echo "ERROR: yt-dlp does not match our pinned hash"; exit 1; }
          sudo install -m 0755 yt-dlp /usr/local/bin/yt-dlp
          rm -f yt-dlp SHA2-256SUMS
          yt-dlp --version

      - name: Build self-contained .deb (all dependencies bundled)
        run: |
          set -e
          PKG=nexa_${NEXA_VERSION}_amd64
          mkdir -p ${PKG}/DEBIAN
          mkdir -p ${PKG}/usr/bin
          mkdir -p ${PKG}/usr/lib/nexa
          mkdir -p ${PKG}/usr/share/applications
          mkdir -p ${PKG}/usr/share/icons/hicolor/256x256/apps
          mkdir -p ${PKG}/usr/share/pixmaps
          mkdir -p ${PKG}/usr/share/nexa

          # ── Binaries ──────────────────────────────────────────────────────
          cp build/nexa       ${PKG}/usr/lib/nexa/nexa
          cp build/nexa-host  ${PKG}/usr/lib/nexa/nexa-host
          chmod +x ${PKG}/usr/lib/nexa/nexa ${PKG}/usr/lib/nexa/nexa-host

          # ── Wrapper scripts in /usr/bin (launch from lib dir) ─────────────
          cat > ${PKG}/usr/bin/nexa <<'SH'
          #!/bin/sh
          exec /usr/lib/nexa/nexa "$@"
          SH
          cat > ${PKG}/usr/bin/nexa-host <<'SH'
          #!/bin/sh
          exec /usr/lib/nexa/nexa-host "$@"
          SH
          chmod +x ${PKG}/usr/bin/nexa ${PKG}/usr/bin/nexa-host

          # ── Bundle the FULL closure gathered by linuxdeploy (every lib + all
          #    Qt plugins) so the app runs on a clean machine with NO Qt or
          #    libtorrent installed. Only glibc + libstdc++/libgcc + the GL stack
          #    stay host-provided (declared as Depends below). ─────────────────
          cp -a AppDir/usr/lib/.   ${PKG}/usr/lib/nexa/
          cp -a AppDir/usr/plugins ${PKG}/usr/lib/nexa/plugins

          # A co-located qt.conf points Qt at the bundled plugins no matter how
          # the binary is launched — GUI wrapper, .desktop entry, or nexa-host
          # spawned directly by the browser (none of which set QT_PLUGIN_PATH).
          cat > ${PKG}/usr/lib/nexa/qt.conf <<'QTCONF'
          [Paths]
          Plugins = plugins
          QTCONF

          # rpath $ORIGIN so BOTH binaries find the co-located .so files with no
          # LD_LIBRARY_PATH — essential for nexa-host, which the browser launches
          # straight from the native-host manifest (never through our wrapper).
          patchelf --set-rpath '$ORIGIN' ${PKG}/usr/lib/nexa/nexa
          patchelf --set-rpath '$ORIGIN' ${PKG}/usr/lib/nexa/nexa-host
          # Qt plugins sit in plugins/<type>/ — two dirs below the bundled .so
          # files. linuxdeploy set their rpath for an AppDir layout (../../lib);
          # rewrite it for OUR layout so a dlopen'd plugin (xcb, openssl, sqlite)
          # finds libxcb / libssl / libQt6* in ../.. . Without this the plugins
          # load nothing on a clean machine — the verify-offline job asserts it.
          find ${PKG}/usr/lib/nexa/plugins -name '*.so' -type f \
            -exec patchelf --set-rpath '$ORIGIN/../..' {} +
          # Every bundled top-level lib also gets $ORIGIN so a lib's OWN deps (e.g.
          # libfontconfig -> libfreetype, libharfbuzz) resolve to co-located libs.
          # DT_RUNPATH isn't transitive, so each object must carry its own rpath.
          for so in ${PKG}/usr/lib/nexa/*.so*; do
            [ -f "$so" ] && patchelf --set-rpath '$ORIGIN' "$so" 2>/dev/null || true
          done

          # ── yt-dlp + ffmpeg (self-contained, no apt needed) ───────────────
          cp /usr/local/bin/yt-dlp ${PKG}/usr/lib/nexa/yt-dlp
          chmod +x ${PKG}/usr/lib/nexa/yt-dlp

          # IMPORTANT: do NOT `cp /usr/bin/ffmpeg` here. Ubuntu's ffmpeg is
          # DYNAMICALLY linked against libavdevice/libavfilter/libavformat/
          # libavcodec/libpostproc/libswresample/libswscale/libavutil, and the
          # linuxdeploy pass above only walks the closure of nexa / nexa-host —
          # never ffmpeg — so none of those eight ever got bundled. The copied
          # binary could not start on ANY machine ("libavdevice.so.60: cannot
          # open shared object file"), yt-dlp reported `exe versions: none`, and
          # every YouTube download ended as two unmerged files on disk
          # (…f602.mp4 + …f233.mp4) instead of one video. HLS muxing and audio
          # extraction broke the same way. This is the identical mistake the
          # Windows job documents at "Bundle ffmpeg (static, self-contained)";
          # it was fixed there and left standing here, so Linux kept the bug.
          # The `2>/dev/null || true` on the old copy meant it could never fail
          # loudly either.
          #
          # Same pinned BtbN release as Windows, linux64-gpl = static. ffprobe
          # too: yt-dlp probes with it and falls back to ffmpeg without it.
          curl -sSL --fail \
            "https://github.com/BtbN/FFmpeg-Builds/releases/download/${FFMPEG_TAG}/${FFMPEG_ASSET_LINUX}" \
            -o ffmpeg-linux.tar.xz
          echo "${FFMPEG_SHA256_LINUX}  ffmpeg-linux.tar.xz" | sha256sum -c - \
            || { echo "ERROR: ffmpeg archive failed its checksum"; exit 1; }
          tar -xf ffmpeg-linux.tar.xz --strip-components=2 \
            -C ${PKG}/usr/lib/nexa/ \
            "${FFMPEG_ASSET_LINUX%.tar.xz}/bin/ffmpeg" \
            "${FFMPEG_ASSET_LINUX%.tar.xz}/bin/ffprobe"
          rm ffmpeg-linux.tar.xz
          chmod +x ${PKG}/usr/lib/nexa/ffmpeg ${PKG}/usr/lib/nexa/ffprobe

          # Sanity-gate: a static build resolves every library and RUNS. Both are
          # asserted, because "the file exists" is exactly what the old copy
          # satisfied while being unable to execute. patchelf must not touch
          # these — the loop above globs *.so* only, so it already doesn't.
          for tool in ffmpeg ffprobe; do
            if ldd ${PKG}/usr/lib/nexa/$tool 2>/dev/null | grep -q "not found"; then
              echo "ERROR: bundled $tool has unresolved libraries — not self-contained"
              ldd ${PKG}/usr/lib/nexa/$tool | grep "not found"
              exit 1
            fi
            ${PKG}/usr/lib/nexa/$tool -version >/dev/null 2>&1 \
              || { echo "ERROR: bundled $tool cannot execute"; exit 1; }
          done
          echo "ffmpeg + ffprobe are static / self-contained and run"

          # ── Icons & desktop entry ─────────────────────────────────────────
          cp assets/nexa-256.png ${PKG}/usr/share/icons/hicolor/256x256/apps/nexa.png
          cp assets/nexa.png     ${PKG}/usr/share/pixmaps/nexa.png

          cat > ${PKG}/usr/share/applications/nexa.desktop <<'DESK'
          [Desktop Entry]
          Type=Application
          Version=1.0
          Name=Nexa Download Manager
          GenericName=Download Manager
          Comment=Fast multi-protocol download manager
          Exec=/usr/bin/nexa %U
          TryExec=/usr/bin/nexa
          Icon=nexa
          Terminal=false
          Categories=Network;FileTransfer;Qt;
          Keywords=download;torrent;video;hls;
          StartupNotify=true
          StartupWMClass=nexa
          DESK

          # ── Extensions — all 4 browsers ──────────────────────────────────
          # Chrome / Edge / Brave (Chromium-based — same MV3 extension)
          cp -r extension-chromium ${PKG}/usr/share/nexa/extension-chromium
          # Firefox (MV2, browser_specific_settings, background script)
          cp -r extension-firefox  ${PKG}/usr/share/nexa/extension-firefox

          # ── Native messaging manifests ────────────────────────────────────
          CHROME_ORIGINS='["chrome-extension://cbogjffoidaepbcbogbfibnldhkckhpb/"]'
          CHROME_JSON="{\"name\":\"com.nexa.host\",\"description\":\"Nexa Download Manager\",\"path\":\"/usr/lib/nexa/nexa-host\",\"type\":\"stdio\",\"allowed_origins\":${CHROME_ORIGINS}}"
          FIREFOX_JSON='{"name":"com.nexa.host","description":"Nexa Download Manager","path":"/usr/lib/nexa/nexa-host","type":"stdio","allowed_extensions":["nexa@nexa.local"]}'

          # Google Chrome
          mkdir -p ${PKG}/etc/opt/chrome/native-messaging-hosts
          echo "$CHROME_JSON" > ${PKG}/etc/opt/chrome/native-messaging-hosts/com.nexa.host.json

          # Chromium
          mkdir -p ${PKG}/etc/chromium/native-messaging-hosts
          echo "$CHROME_JSON" > ${PKG}/etc/chromium/native-messaging-hosts/com.nexa.host.json

          # Microsoft Edge
          mkdir -p ${PKG}/etc/opt/edge/native-messaging-hosts
          echo "$CHROME_JSON" > ${PKG}/etc/opt/edge/native-messaging-hosts/com.nexa.host.json

          # Brave Browser
          mkdir -p "${PKG}/etc/opt/brave/native-messaging-hosts"
          echo "$CHROME_JSON" > "${PKG}/etc/opt/brave/native-messaging-hosts/com.nexa.host.json"

          # Firefox
          mkdir -p ${PKG}/usr/lib/mozilla/native-messaging-hosts
          echo "$FIREFOX_JSON" > ${PKG}/usr/lib/mozilla/native-messaging-hosts/com.nexa.host.json

          # ── Launcher ──────────────────────────────────────────────────────
          # Deliberately NO LD_LIBRARY_PATH. The rpath pass above gives both
          # binaries $ORIGIN and the plugins $ORIGIN/../.., so every bundled .so
          # already resolves without it — that is exactly what the comment there
          # says it is for. Exporting it anyway was a leftover, and it is not
          # harmless: the environment is inherited by every child process, so
          # our OpenSSL shadowed the system one for the SYSTEM python that runs
          # yt-dlp, and importing ssl died with
          #   ImportError: /usr/lib/nexa/libcrypto.so.3: version
          #   `OPENSSL_3.3.0' not found (required by …/_ssl…so)
          # before a single byte was fetched. The bundled libcrypto is built for
          # the runner's distro and cannot satisfy an arbitrary host's python.
          # PATH stays: it only affects which executables are found, never which
          # libraries are loaded, and it is how a child reaches bundled yt-dlp.
          cat > ${PKG}/usr/bin/nexa <<'SH'
          #!/bin/sh
          export PATH=/usr/lib/nexa:$PATH
          exec /usr/lib/nexa/nexa "$@"
          SH
          chmod +x ${PKG}/usr/bin/nexa

          # ── Postinst: update icon/desktop caches + aria2 install ──────────
          cat > ${PKG}/DEBIAN/postinst <<'POST'
          #!/bin/sh
          update-desktop-database /usr/share/applications 2>/dev/null || true
          gtk-update-icon-cache /usr/share/icons/hicolor 2>/dev/null || true
          echo ""
          echo "=========================================="
          echo "  Nexa Download Manager installed!"
          echo "=========================================="
          echo ""
          echo "Load browser extension:"
          echo ""
          echo "  Chrome / Edge / Brave:"
          echo "    1. Open chrome://extensions (or edge://extensions / brave://extensions)"
          echo "    2. Enable Developer mode"
          echo "    3. Load unpacked -> /usr/share/nexa/extension-chromium"
          echo ""
          echo "  Firefox:"
          echo "    1. Open about:debugging -> This Firefox"
          echo "    2. Load Temporary Add-on -> /usr/share/nexa/extension-firefox/manifest.json"
          echo "    (For permanent: sign & submit to addons.mozilla.org)"
          echo ""
          echo "Native messaging host is auto-registered for all browsers."
          echo "=========================================="
          POST
          chmod 755 ${PKG}/DEBIAN/postinst

          # ── control file — NO external dependencies required ──────────────
          INSTALLED_SIZE=$(du -sk ${PKG} | cut -f1)
          cat > ${PKG}/DEBIAN/control <<CTRL
          Package: nexa
          Version: ${NEXA_VERSION}
          Architecture: amd64
          Maintainer: Nexa <tauseeqtufail649@gmail.com>
          Installed-Size: ${INSTALLED_SIZE}
          Depends: libc6, libstdc++6, libgcc-s1, libgl1, libglx0, libopengl0, libegl1, python3
          Description: Nexa Download Manager
           Fast multi-protocol download manager with browser integration.
           Supports HTTP/HTTPS segmented downloads, HLS video streams,
           BitTorrent, and site-specific media via yt-dlp.
           Includes yt-dlp and ffmpeg — no extra installs needed.
          CTRL

          # ── Build the package ─────────────────────────────────────────────
          # --root-owner-group: without it dpkg-deb stamps the STAGING tree's
          # ownership into the archive, which on a runner is uid 1001, and dpkg
          # faithfully installs /usr/lib/nexa/* owned by 1001 on the user's
          # machine. Beyond being wrong for system files, it silently disabled
          # launching from the browser: nexa-host refuses to exec an engine that
          # is owned by neither the calling user nor root (a link/replace
          # guard), so on any machine whose user is not uid 1001 — i.e. almost
          # all of them, since the first account is normally 1000 — the handoff
          # only worked if Nexa was ALREADY open. Owned by root and mode 755 it
          # passes both that check and the companion "not group/other writable"
          # one.
          dpkg-deb --root-owner-group --build ${PKG}
          ls -lh ${PKG}.deb
          # Assert it, because the symptom (browser handoff does nothing unless
          # the app is already running) looks like an IPC bug, not a chown one.
          dpkg-deb -c ${PKG}.deb | grep -E ' \./usr/lib/nexa/nexa$' \
            | grep -q '^[^ ]* *root/root ' \
            || { echo "ERROR: engine is not root-owned — browser launch would be refused"; \
                 dpkg-deb -c ${PKG}.deb | grep ' \./usr/lib/nexa/nexa$'; exit 1; }
          echo "✅ Self-contained .deb ready: $(du -sh ${PKG}.deb | cut -f1)"

      - name: Upload .deb artifact
        uses: actions/upload-artifact@v7
        with:
          name: nexa-linux-amd64-deb
          path: nexa_${{ env.NEXA_VERSION }}_amd64.deb
          retention-days: 90

      - name: Create GitHub Release (Linux .deb)
        if: github.ref == 'refs/heads/main'
        continue-on-error: true
        uses: softprops/action-gh-release@v3
        with:
          tag_name: build-${{ github.run_number }}
          name: "Nexa Download Manager — Build ${{ github.run_number }}"
          body: |
            ## Nexa Download Manager

            ### Linux (.deb — Ubuntu/Debian/Kali):
            ```bash
            sudo dpkg -i nexa_*.deb
            sudo apt-get install -f   # fix any missing deps
            ```

            ### Windows (.exe installer):
            - Run `NexaSetup.exe` as administrator
            - Load Chrome extension from `C:\Program Files\Nexa Download Manager\extension-chromium`

            Built from: ${{ github.sha }}
          files: |
            nexa_${{ env.NEXA_VERSION }}_amd64.deb
          draft: false
          prerelease: false
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

  # ── Windows self-contained installer via MSYS2/MinGW ──────────────────────
  # MSYS2 has pre-built Qt6 + libtorrent + ffmpeg — no vcpkg compile needed.
  windows:
    name: Windows installer (MSYS2 + full torrent)
    runs-on: windows-latest
    timeout-minutes: 45
    if: github.event_name != 'pull_request'
    defaults:
      run:
        shell: msys2 {0}

    steps:
      - uses: actions/checkout@v7

      - name: Setup MSYS2 + MinGW64
        uses: msys2/setup-msys2@v2
        with:
          msystem: MINGW64
          update: true
          install: >-
            mingw-w64-x86_64-gcc
            mingw-w64-x86_64-cmake
            mingw-w64-x86_64-ninja
            mingw-w64-x86_64-qt6-base
            mingw-w64-x86_64-qt6-tools
            mingw-w64-x86_64-libtorrent-rasterbar
            mingw-w64-x86_64-pkg-config
            unzip

      - name: Configure
        run: |
          cmake -B build -G Ninja \
            -DCMAKE_BUILD_TYPE=Release \
            -DCMAKE_PREFIX_PATH=/mingw64

      - name: Build
        run: cmake --build build

      - name: Verify binaries
        run: |
          ls -lh build/nexa.exe build/nexa-host.exe
          echo "Build OK"

      - name: Bundle app + Qt DLLs
        run: |
          set -e
          mkdir -p dist

          # Copy binaries
          cp build/nexa.exe      dist/
          cp build/nexa-host.exe dist/

          # Use ldd to find all required DLLs automatically (no hardcoded paths)
          echo "=== Collecting DLLs via ldd ==="
          ldd build/nexa.exe | grep -i mingw64 | awk '{print $3}' | xargs -I{} cp -n {} dist/ 2>/dev/null || true
          ldd build/nexa-host.exe | grep -i mingw64 | awk '{print $3}' | xargs -I{} cp -n {} dist/ 2>/dev/null || true

          # Qt plugins (use MSYS2 path, works regardless of install location)
          QTPLUGINS=$(qmake6 -query QT_INSTALL_PLUGINS 2>/dev/null || echo "/mingw64/share/qt6/plugins")
          mkdir -p dist/platforms dist/sqldrivers dist/styles
          cp "${QTPLUGINS}/platforms/qwindows.dll"  dist/platforms/ 2>/dev/null || \
          cp /mingw64/share/qt6/plugins/platforms/qwindows.dll dist/platforms/
          cp "${QTPLUGINS}/sqldrivers/qsqlite.dll"  dist/sqldrivers/ 2>/dev/null || \
          cp /mingw64/share/qt6/plugins/sqldrivers/qsqlite.dll dist/sqldrivers/
          cp /mingw64/share/qt6/plugins/styles/qwindowsvistastyle.dll dist/styles/ 2>/dev/null || true

          # The QSQLITE driver plugin (qsqlite.dll) is loaded at RUNTIME and depends
          # on libsqlite3-0.dll — a dep of the PLUGIN, not of nexa.exe — so the
          # ldd-on-exe pass missed it and Qt reported
          # "QSqlDatabase: can not load requested driver 'QSQLITE'". Pull the
          # plugin's own deps + glob libsqlite3 directly.
          ldd dist/sqldrivers/qsqlite.dll 2>/dev/null | grep -i mingw64 | awk '{print $3}' | xargs -I{} cp -n {} dist/ 2>/dev/null || true
          cp -n /mingw64/bin/libsqlite3-*.dll dist/ 2>/dev/null || true

          # Qt TLS backends are loaded at RUNTIME via the plugin loader, so `ldd`
          # on the exe never lists them. Without this folder Qt prints
          # "No functional TLS backend was found" and every https request fails
          # on a clean PC. Bundle the whole tls/ dir (schannel + openssl backends).
          mkdir -p dist/tls
          cp "${QTPLUGINS}/tls/"*.dll dist/tls/ 2>/dev/null || \
          cp /mingw64/share/qt6/plugins/tls/*.dll dist/tls/
          # The openssl backend depends on libssl/libcrypto, which are deps of the
          # PLUGIN (not of nexa.exe), so the ldd-on-exe pass above missed them.
          # Resolve the plugin's own deps, plus a direct glob as a fallback.
          ldd dist/tls/qopensslbackend.dll 2>/dev/null | grep -i mingw64 | awk '{print $3}' | xargs -I{} cp -n {} dist/ 2>/dev/null || true
          cp -n /mingw64/bin/libssl-*.dll    dist/ 2>/dev/null || true
          cp -n /mingw64/bin/libcrypto-*.dll dist/ 2>/dev/null || true
          echo "=== tls backends bundled ===" && ls dist/tls/

          # Image-format plugins are ALSO runtime-loaded (ldd misses them). PNG is
          # built into Qt6Gui, but SVG/JPEG/ICO are not — without these a clean PC
          # shows broken icons/logo. Bundle the folder + pull each plugin's own deps
          # (e.g. qsvg.dll needs Qt6Svg.dll, which nexa.exe may not link directly).
          mkdir -p dist/imageformats
          cp "${QTPLUGINS}/imageformats/"*.dll dist/imageformats/ 2>/dev/null || \
          cp /mingw64/share/qt6/plugins/imageformats/*.dll dist/imageformats/ 2>/dev/null || true
          for p in dist/imageformats/*.dll; do
            ldd "$p" 2>/dev/null | grep -i mingw64 | awk '{print $3}' | xargs -I{} cp -n {} dist/ 2>/dev/null || true
          done
          echo "=== imageformats bundled ===" && ls dist/imageformats/

          echo "dist contents: $(ls dist/ | wc -l) files"
          ls dist/*.dll | head -20

      - name: Bundle yt-dlp
        shell: pwsh
        run: |
          $ErrorActionPreference = 'Stop'
          Invoke-WebRequest `
            -Uri "https://github.com/yt-dlp/yt-dlp/releases/download/$env:YTDLP_VERSION/yt-dlp.exe" `
            -OutFile dist\yt-dlp.exe
          # This binary goes inside the installer we Authenticode-sign, so it is
          # checked before it gets there.
          $got = (Get-FileHash dist\yt-dlp.exe -Algorithm SHA256).Hash.ToLower()
          if ($got -ne $env:YTDLP_SHA256_WINDOWS) {
            throw "yt-dlp.exe checksum mismatch: got $got, expected $env:YTDLP_SHA256_WINDOWS"
          }
          Write-Host "yt-dlp $env:YTDLP_VERSION bundled and verified"

      - name: Bundle ffmpeg (static, self-contained)
        run: |
          # IMPORTANT: do NOT bundle MSYS2's /mingw64/bin/ffmpeg.exe. It is
          # DYNAMICALLY linked against dozens of mingw64 libav*/codec DLLs
          # (libavcodec, libavformat, libswresable, libx264, libvpx, libopus, …)
          # that we do NOT ship — the DLL-collection pass above only runs `ldd` on
          # nexa.exe / nexa-host.exe, never on ffmpeg.exe. On a clean PC the bundled
          # ffmpeg then fails to start (missing DLLs), so yt-dlp cannot merge the
          # separate video+audio streams and leaves two unmerged files on disk
          # (and HLS muxing / Apple-Music audio extraction break the same way).
          #
          # The BtbN "win64-gpl" build is a STATIC, self-contained ffmpeg.exe with
          # no external DLL dependencies — the correct thing to bundle. Grab ffprobe
          # too (yt-dlp uses it for format/duration probing).
          curl -sSL --fail "https://github.com/BtbN/FFmpeg-Builds/releases/download/${FFMPEG_TAG}/${FFMPEG_ASSET}" \
            -o ffmpeg.zip
          # Verified before we unzip it: this ships inside the signed installer.
          echo "$FFMPEG_SHA256  ffmpeg.zip" | sha256sum -c - \
            || { echo "ERROR: ffmpeg archive failed its checksum"; exit 1; }
          unzip -j ffmpeg.zip "*/ffmpeg.exe" "*/ffprobe.exe" -d dist/
          rm ffmpeg.zip
          ls -lh dist/ffmpeg.exe dist/ffprobe.exe
          # Sanity-gate: a static build must have ZERO mingw64 DLL deps. If `ldd`
          # ever lists one, the bundle would be broken on a clean PC — fail loudly
          # so this regression can never ship silently again.
          if ldd dist/ffmpeg.exe 2>/dev/null | grep -iq mingw64; then
            echo "ERROR: bundled ffmpeg.exe has mingw64 DLL deps — not self-contained"; exit 1
          fi
          echo "ffmpeg.exe is static / self-contained"

      - name: Bundle extensions (Chrome/Edge/Brave + Firefox)
        run: |
          cp -r extension-chromium dist/extension-chromium
          cp -r extension-firefox  dist/extension-firefox

      - name: Write native host manifest
        run: |
          # `path` is RELATIVE — Chrome/Chromium on Windows resolve a native-host
          # manifest path relative to the manifest file's own directory, and
          # com.nexa.host.json sits next to nexa-host.exe in $INSTDIR. This avoids
          # baking an install path (and its JSON backslash-escaping) into NSIS.
          # The old "INSTDIR\\nexa-host.exe" placeholder was never substituted, so
          # Chrome reported "Specified native messaging host not found".
          echo '{"name":"com.nexa.host","description":"Nexa Download Manager","path":"nexa-host.exe","type":"stdio","allowed_origins":["chrome-extension://cbogjffoidaepbcbogbfibnldhkckhpb/"]}' \
            > dist/manifest_template.json

      - name: Install NSIS
        shell: pwsh
        run: choco install nsis --no-progress -y

      - name: Generate NSIS installer script
        shell: pwsh
        run: |
          # Browser-extension auto-install: one config file drives the app, the
          # .deb and this installer. Chrome-family browsers read an "external
          # extension" registry entry and install from the store on next start;
          # Firefox reads an Extensions.Install policy pointing at a signed .xpi.
          $ids = @{}
          Get-Content packaging/extension-ids.env | ForEach-Object {
            if ($_ -match '^\s*([A-Z_]+)\s*=\s*(.*?)\s*$') { $ids[$Matches[1]] = $Matches[2].Trim('"', "'") }
          }
          $chromeId  = $ids['NEXA_CHROME_STORE_ID']
          $edgeId    = $ids['NEXA_EDGE_STORE_ID']
          $xpiUrl    = $ids['NEXA_FIREFOX_XPI_URL']
          $chromeUpd = 'https://clients2.google.com/service/update2/crx'
          $edgeUpd   = 'https://edge.microsoft.com/extensionwebstorebase/v1/crx'
          $extensionInstallLines = @()
          $extensionRemoveLines  = @()
          if ($chromeId -match '^[a-p]{32}$') {
            foreach ($vendor in @('Google\Chrome', 'Chromium', 'BraveSoftware\Brave-Browser', 'Vivaldi')) {
              $extensionInstallLines += "  WriteRegStr HKLM `"Software\$vendor\Extensions\$chromeId`" `"update_url`" `"$chromeUpd`""
              $extensionRemoveLines  += "  DeleteRegKey HKLM `"Software\$vendor\Extensions\$chromeId`""
            }
          }
          if ($edgeId -match '^[a-p]{32}$') {
            $extensionInstallLines += "  WriteRegStr HKLM `"Software\Microsoft\Edge\Extensions\$edgeId`" `"update_url`" `"$edgeUpd`""
            $extensionRemoveLines  += "  DeleteRegKey HKLM `"Software\Microsoft\Edge\Extensions\$edgeId`""
          } elseif ($chromeId -match '^[a-p]{32}$') {
            $extensionInstallLines += "  WriteRegStr HKLM `"Software\Microsoft\Edge\Extensions\$chromeId`" `"update_url`" `"$chromeUpd`""
            $extensionRemoveLines  += "  DeleteRegKey HKLM `"Software\Microsoft\Edge\Extensions\$chromeId`""
          }
          if ($xpiUrl -like 'https://*') {
            $extensionInstallLines += "  WriteRegStr HKLM `"Software\Policies\Mozilla\Firefox\Extensions\Install`" `"1`" `"$xpiUrl`""
            $extensionRemoveLines  += "  DeleteRegValue HKLM `"Software\Policies\Mozilla\Firefox\Extensions\Install`" `"1`""
          }
          Write-Host "Extension auto-install lines: $($extensionInstallLines.Count)"
          $lines = @(
            '!include "MUI2.nsh"',
            'Name "Nexa Download Manager"',
            'OutFile "NexaSetup.exe"',
            'Unicode True',
            'InstallDir "$PROGRAMFILES64\Nexa Download Manager"',
            'RequestExecutionLevel admin',
            '!define MUI_ABORTWARNING',
            '!insertmacro MUI_PAGE_WELCOME',
            '!insertmacro MUI_PAGE_DIRECTORY',
            '!insertmacro MUI_PAGE_INSTFILES',
            '; NOTE: no MUI_FINISHPAGE_RUN. This installer is elevated (admin), so a',
            '; finish-page launch would start nexa.exe at admin integrity. That nexa',
            '; would create the "nexa-ipc" named pipe with a per-user (admin) DACL, and',
            '; the browser-spawned nexa-host.exe (running as the normal logged-in user)',
            '; could not connect to it. Let the user launch via the shortcut instead;',
            '; the host also launches nexa.exe on demand, both at the correct integrity.',
            '!insertmacro MUI_PAGE_FINISH',
            '!insertmacro MUI_UNPAGE_CONFIRM',
            '!insertmacro MUI_UNPAGE_INSTFILES',
            '!insertmacro MUI_LANGUAGE "English"',
            '',
            'Section "Install"',
            '  SetOutPath "$INSTDIR"',
            '  ; Write to the native 64-bit registry view. makensis produces a 32-bit',
            '  ; installer; without this, HKLM\Software\* writes are redirected into',
            '  ; HKLM\Software\WOW6432Node\* — where 64-bit Chrome/Edge/Brave never look,',
            '  ; so the native host would be invisible even though the keys "exist".',
            '  SetRegView 64',
            '  File /r "dist\*"',
            '  ; Write native messaging host manifest',
            '  FileOpen $0 "$INSTDIR\manifest_template.json" r',
            '  FileOpen $1 "$INSTDIR\com.nexa.host.json" w',
            '  loop:',
            '    FileRead $0 $2',
            '    IfErrors done',
            '    FileWrite $1 $2',
            '    Goto loop',
            '  done:',
            '  FileClose $0',
            '  FileClose $1',
            '  Delete "$INSTDIR\manifest_template.json"',
            '  ; Register the native host MACHINE-WIDE (HKLM), for Chrome, Chromium,',
            '  ; Edge, Brave. This installer is elevated and installs to Program Files,',
            '  ; so HKLM is the correct, all-users hive — and crucially it is the hive',
            '  ; the logged-in account Chrome actually reads. Writing HKCU from an',
            '  ; elevated installer lands in the admin hive, invisible to the real',
            '  ; user, which is why the extension reported "native host not found".',
            '  WriteRegStr HKLM "Software\Google\Chrome\NativeMessagingHosts\com.nexa.host" "" "$INSTDIR\com.nexa.host.json"',
            '  WriteRegStr HKLM "Software\Chromium\NativeMessagingHosts\com.nexa.host" "" "$INSTDIR\com.nexa.host.json"',
            '  WriteRegStr HKLM "Software\Microsoft\Edge\NativeMessagingHosts\com.nexa.host" "" "$INSTDIR\com.nexa.host.json"',
            '  WriteRegStr HKLM "Software\BraveSoftware\Brave-Browser\NativeMessagingHosts\com.nexa.host" "" "$INSTDIR\com.nexa.host.json"',
            '  ; Hand every Chromium browser and Firefox the extension itself, from its',
            '  ; store ("external extension" entries / a Firefox install policy). The ids',
            '  ; come from packaging/extension-ids.env; an empty id emits nothing.'
          ) + $extensionInstallLines + @(
            '  WriteRegStr HKLM "Software\Microsoft\Windows\CurrentVersion\Run" "NexaDL" "$\"$INSTDIR\nexa.exe$\" --background"',
            '  WriteRegStr HKLM "Software\Microsoft\Windows\CurrentVersion\Uninstall\NexaDL" "DisplayName" "Nexa Download Manager"',
            '  WriteRegStr HKLM "Software\Microsoft\Windows\CurrentVersion\Uninstall\NexaDL" "UninstallString" "$\"$INSTDIR\uninstall.exe$\""',
            '  WriteRegStr HKLM "Software\Microsoft\Windows\CurrentVersion\Uninstall\NexaDL" "DisplayIcon" "$INSTDIR\nexa.exe"',
            '  WriteUninstaller "$INSTDIR\uninstall.exe"',
            '  CreateDirectory "$SMPROGRAMS\Nexa Download Manager"',
            '  CreateShortCut "$SMPROGRAMS\Nexa Download Manager\Nexa.lnk" "$INSTDIR\nexa.exe"',
            '  CreateShortCut "$DESKTOP\Nexa Download Manager.lnk" "$INSTDIR\nexa.exe"',
            'SectionEnd',
            '',
            'Section "Uninstall"',
            '  ; Must match the install view (64-bit) and hive (HKLM) used above,',
            '  ; otherwise these deletes target the wrong, empty keys and leave the',
            '  ; real registrations behind.',
            '  SetRegView 64',
            '  DeleteRegValue HKLM "Software\Microsoft\Windows\CurrentVersion\Run" "NexaDL"',
            '  DeleteRegKey HKLM "Software\Google\Chrome\NativeMessagingHosts\com.nexa.host"',
            '  DeleteRegKey HKLM "Software\Chromium\NativeMessagingHosts\com.nexa.host"',
            '  DeleteRegKey HKLM "Software\Microsoft\Edge\NativeMessagingHosts\com.nexa.host"',
            '  DeleteRegKey HKLM "Software\BraveSoftware\Brave-Browser\NativeMessagingHosts\com.nexa.host"'
          ) + $extensionRemoveLines + @(
            '  DeleteRegKey HKLM "Software\Microsoft\Windows\CurrentVersion\Uninstall\NexaDL"',
            '  RMDir /r "$INSTDIR"',
            '  Delete "$DESKTOP\Nexa Download Manager.lnk"',
            '  RMDir /r "$SMPROGRAMS\Nexa Download Manager"',
            'SectionEnd'
          )
          $lines | Set-Content installer.nsi -Encoding UTF8
          Write-Host "NSIS script written ($($lines.Count) lines)"

      - name: Build installer
        shell: pwsh
        run: |
          & "C:\Program Files (x86)\NSIS\makensis.exe" installer.nsi
          $mb = [math]::Round((Get-Item NexaSetup.exe).Length/1MB, 1)
          Write-Host "NexaSetup.exe ready: $mb MB"

      # Authenticode signing. Without it every download shows a SmartScreen
      # "unknown publisher" warning, and — the reason it belongs in this
      # workflow — a cracked build repackaged by someone else is indistinguishable
      # from the real one to the person installing it. A signature is what makes
      # tampering visible.
      #
      # Runs only when the certificate secrets exist, so forks and PRs still
      # build. Set WINDOWS_CERT_P12 (base64 of the .pfx) and WINDOWS_CERT_PASSWORD
      # in the repository secrets to turn it on.
      - name: Sign installer
        shell: pwsh
        env:
          WINDOWS_CERT_P12: ${{ secrets.WINDOWS_CERT_P12 }}
          WINDOWS_CERT_PASSWORD: ${{ secrets.WINDOWS_CERT_PASSWORD }}
        run: |
          if (-not $env:WINDOWS_CERT_P12) {
            Write-Host "::warning::No WINDOWS_CERT_P12 secret - shipping an UNSIGNED installer. Users will see a SmartScreen warning and a tampered rebuild is indistinguishable from this one."
            exit 0
          }
          $ErrorActionPreference = 'Stop'
          $pfx = Join-Path $env:RUNNER_TEMP 'nexa-signing.pfx'
          [IO.File]::WriteAllBytes($pfx, [Convert]::FromBase64String($env:WINDOWS_CERT_P12))
          try {
            $signtool = Get-ChildItem 'C:\Program Files (x86)\Windows Kits\10\bin' -Recurse -Filter signtool.exe |
              Where-Object { $_.FullName -match 'x64' } | Select-Object -First 1
            if (-not $signtool) { throw "signtool.exe not found on the runner" }
            # RFC 3161 timestamp so the signature stays valid after the
            # certificate expires.
            & $signtool.FullName sign `
              /f $pfx /p $env:WINDOWS_CERT_PASSWORD `
              /fd SHA256 /tr http://timestamp.digicert.com /td SHA256 `
              /d "Nexa Download Manager" `
              NexaSetup.exe
            if ($LASTEXITCODE -ne 0) { throw "signtool failed with $LASTEXITCODE" }
            & $signtool.FullName verify /pa /v NexaSetup.exe
            if ($LASTEXITCODE -ne 0) { throw "signature verification failed" }
            Write-Host "Installer signed and verified."
          } finally {
            Remove-Item $pfx -Force -ErrorAction SilentlyContinue
          }

      - name: Upload artifact
        uses: actions/upload-artifact@v7
        with:
          name: NexaSetup-Windows-x64
          path: NexaSetup.exe
          retention-days: 90

      - name: Create GitHub Release
        if: github.ref == 'refs/heads/main'
        continue-on-error: true   # Don't fail the build if release permissions are missing
        uses: softprops/action-gh-release@v3
        with:
          tag_name: build-${{ github.run_number }}
          name: "Nexa Download Manager — Build ${{ github.run_number }}"
          body: |
            ## Nexa Download Manager — Windows Installer

            ### Install karna (sirf 2 steps):
            1. `NexaSetup.exe` download karein → **Run as administrator** → Install
            2. Chrome: `chrome://extensions/` → Developer mode ON → **Load unpacked**
               → `C:\Program Files\Nexa Download Manager\extension-chromium`

            **Included:** nexa.exe · yt-dlp · ffmpeg · libtorrent · Chrome extension · native host auto-registered

            Built from: ${{ github.sha }}
          files: NexaSetup.exe
          draft: false
          prerelease: false
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

  # ── Prove the .deb is fully self-contained ────────────────────────────────
  # Install it in a CLEAN ubuntu:24.04 container (same glibc as the builder, but
  # NO Qt / libtorrent / fontconfig / xcb — nothing the app needs). Then assert
  # every bundled binary AND every bundled Qt plugin resolves all its libraries.
  # If the build weren't self-contained, ldd would print "not found" and we fail.
  # glibc + libstdc++/libgcc + the GL stack come from the base image by design —
  # those are the libraries you cannot relocate.
  # ── macOS CI ───────────────────────────────────────────────────────────────
  # Signs and notarises when the Developer ID secrets are configured
  # (MACOS_CERT_P12 / MACOS_CERT_PASSWORD / MACOS_SIGN_IDENTITY, plus
  # APPLE_ID / APPLE_TEAM_ID / APPLE_APP_PASSWORD for notarisation). Without
  # them it still builds, but produces an UNSIGNED bundle that Gatekeeper will
  # refuse — a developer/preview build, not a public download.
  macos:
    name: macOS app bundle
    runs-on: macos-latest
    # Notarisation waits on Apple's service, which can take a while on its own.
    timeout-minutes: 90
    steps:
      - uses: actions/checkout@v7

      - name: Install toolchain
        run: |
          brew update
          brew install qt@6 libtorrent-rasterbar boost ninja openssl@3 create-dmg || true
          echo "$(brew --prefix qt@6)/bin" >> $GITHUB_PATH

      # CMAKE_CXX_FLAGS carries boost's include dir because Homebrew's
      # libtorrent-rasterbar.pc lists only its OWN includedir, while libtorrent's
      # public headers open with #include <boost/config.hpp>. Boost is a separate
      # keg under /opt/homebrew/opt/boost, so pkg-config's Cflags leave the
      # compiler unable to find it and TorrentManager.cpp fails to compile.
      # CMAKE_PREFIX_PATH alone does not fix this: it steers find_package(), not
      # the include path of a target found through pkg_check_modules.
      - name: Configure & build
        run: |
          cmake -B build -G Ninja \
            -DCMAKE_BUILD_TYPE=Release \
            -DCMAKE_PREFIX_PATH="$(brew --prefix qt@6);$(brew --prefix libtorrent-rasterbar);$(brew --prefix boost);$(brew --prefix openssl@3)" \
            -DCMAKE_CXX_FLAGS="-I$(brew --prefix boost)/include"
          cmake --build build

      - name: Smoke test
        timeout-minutes: 5
        run: |
          test -d build/nexa.app && echo "bundle OK"
          test -x build/nexa-host && echo "host OK"
          # --version prints and exits before any window, socket or hook exists.
          # This used to run --help, which the app did not handle: it launched in
          # full and the job sat here until GitHub's 6-hour limit.
          ./build/nexa.app/Contents/MacOS/nexa --version

      - name: Run unit tests
        run: |
          cmake -B build-tests -G Ninja -DNEXA_BUILD_TESTS=ON \
            -DCMAKE_PREFIX_PATH="$(brew --prefix qt@6);$(brew --prefix libtorrent-rasterbar);$(brew --prefix boost);$(brew --prefix openssl@3)" \
            -DCMAKE_CXX_FLAGS="-I$(brew --prefix boost)/include"
          cmake --build build-tests
          ctest --test-dir build-tests --output-on-failure

      - name: Bundle Qt frameworks + tools
        run: |
          set -e
          # macdeployqt copies the Qt frameworks and plugins into the .app.
          "$(brew --prefix qt@6)/bin/macdeployqt" build/nexa.app -verbose=1
          # The native-messaging host lives inside the bundle so one drag installs both.
          cp build/nexa-host build/nexa.app/Contents/MacOS/nexa-host
          # yt-dlp + ffmpeg make the video features work without a Homebrew install.
          curl -sSL --fail "https://github.com/yt-dlp/yt-dlp/releases/download/${YTDLP_VERSION}/yt-dlp_macos" \
            -o build/nexa.app/Contents/MacOS/yt-dlp
          # Verified before it is signed into the bundle. macOS ships shasum, not
          # sha256sum.
          echo "$YTDLP_SHA256_MACOS  build/nexa.app/Contents/MacOS/yt-dlp" | shasum -a 256 -c - \
            || { echo "ERROR: yt-dlp_macos failed its checksum"; exit 1; }
          chmod +x build/nexa.app/Contents/MacOS/yt-dlp
          echo "=== bundle contents ==="; ls -la build/nexa.app/Contents/MacOS/

      # Developer ID signing. Runs only when the certificate secrets exist, so
      # forks and PRs still build unsigned. Set MACOS_CERT_P12 (base64 of the
      # Developer ID Application .p12), MACOS_CERT_PASSWORD and
      # MACOS_SIGN_IDENTITY to turn it on.
      #
      # Inner binaries are signed before the bundle: codesign seals what it
      # finds, so anything signed afterwards invalidates the outer signature.
      - name: Sign app bundle
        env:
          MACOS_CERT_P12: ${{ secrets.MACOS_CERT_P12 }}
          MACOS_CERT_PASSWORD: ${{ secrets.MACOS_CERT_PASSWORD }}
          MACOS_SIGN_IDENTITY: ${{ secrets.MACOS_SIGN_IDENTITY }}
        run: |
          set -e
          if [ -z "$MACOS_CERT_P12" ]; then
            echo "::warning::No MACOS_CERT_P12 secret - shipping an UNSIGNED bundle. Gatekeeper will block it, and a tampered rebuild is indistinguishable from this one."
            echo "SIGNED=0" >> "$GITHUB_ENV"
            exit 0
          fi

          KEYCHAIN="$RUNNER_TEMP/nexa-signing.keychain-db"
          KEYCHAIN_PASSWORD="$(uuidgen)"
          security create-keychain -p "$KEYCHAIN_PASSWORD" "$KEYCHAIN"
          security set-keychain-settings -lut 900 "$KEYCHAIN"
          security unlock-keychain -p "$KEYCHAIN_PASSWORD" "$KEYCHAIN"

          echo "$MACOS_CERT_P12" | base64 --decode > "$RUNNER_TEMP/cert.p12"
          security import "$RUNNER_TEMP/cert.p12" -k "$KEYCHAIN" \
            -P "$MACOS_CERT_PASSWORD" -T /usr/bin/codesign
          rm -f "$RUNNER_TEMP/cert.p12"
          security set-key-partition-list -S apple-tool:,apple: \
            -k "$KEYCHAIN_PASSWORD" "$KEYCHAIN" >/dev/null
          security list-keychains -d user -s "$KEYCHAIN" $(security list-keychains -d user | tr -d '"')

          # --options runtime is required for notarisation.
          for inner in build/nexa.app/Contents/MacOS/nexa-host \
                       build/nexa.app/Contents/MacOS/yt-dlp; do
            [ -f "$inner" ] && codesign --force --timestamp --options runtime \
              --sign "$MACOS_SIGN_IDENTITY" "$inner"
          done
          codesign --force --deep --timestamp --options runtime \
            --sign "$MACOS_SIGN_IDENTITY" build/nexa.app
          codesign --verify --deep --strict --verbose=2 build/nexa.app
          echo "SIGNED=1" >> "$GITHUB_ENV"

      - name: Create .dmg
        run: |
          set -e
          mkdir -p dmg-staging
          cp -R build/nexa.app dmg-staging/
          ln -s /Applications dmg-staging/Applications
          hdiutil create -volname "Nexa Download Manager" -srcfolder dmg-staging \
            -ov -format UDZO Nexa-macOS.dmg
          if [ "${SIGNED:-0}" = "1" ]; then
            echo "Signed with a Developer ID. Notarisation status is reported in the build log." \
              > Nexa-macOS-README.txt
          else
            echo "UNSIGNED build - Gatekeeper will block it until signed/notarised." \
              > Nexa-macOS-README.txt
          fi

      # Notarisation is a separate credential set from signing, so it is a
      # separate step: a repo can sign without notarising while Apple
      # credentials are being sorted out. Stapling lets the .dmg pass Gatekeeper
      # offline.
      - name: Notarise .dmg
        if: env.SIGNED == '1'
        env:
          APPLE_ID: ${{ secrets.APPLE_ID }}
          APPLE_TEAM_ID: ${{ secrets.APPLE_TEAM_ID }}
          APPLE_APP_PASSWORD: ${{ secrets.APPLE_APP_PASSWORD }}
          MACOS_SIGN_IDENTITY: ${{ secrets.MACOS_SIGN_IDENTITY }}
        run: |
          set -e
          if [ -z "$APPLE_ID" ] || [ -z "$APPLE_APP_PASSWORD" ] || [ -z "$APPLE_TEAM_ID" ]; then
            echo "::warning::Signed but NOT notarised (no Apple ID secrets). Gatekeeper still warns on first open."
            exit 0
          fi
          codesign --force --timestamp --sign "$MACOS_SIGN_IDENTITY" Nexa-macOS.dmg
          xcrun notarytool submit Nexa-macOS.dmg \
            --apple-id "$APPLE_ID" \
            --team-id "$APPLE_TEAM_ID" \
            --password "$APPLE_APP_PASSWORD" \
            --wait
          xcrun stapler staple Nexa-macOS.dmg
          xcrun stapler validate Nexa-macOS.dmg
          echo "Notarised and stapled." > Nexa-macOS-README.txt

      - uses: actions/upload-artifact@v7
        with:
          name: nexa-macos-dmg
          path: |
            Nexa-macOS.dmg
            Nexa-macOS-README.txt

  verify-offline:
    name: Verify .deb is self-contained (clean container)
    needs: linux
    runs-on: ubuntu-latest
    timeout-minutes: 15
    container: ubuntu:24.04
    defaults:
      run:
        shell: bash
    steps:
      - name: Download .deb artifact
        uses: actions/download-artifact@v8
        with:
          name: nexa-linux-amd64-deb

      - name: Install into a bare system
        run: |
          set -e
          apt-get update
          # Install ONLY the .deb and whatever it declares (libc6, libstdc++6,
          # libgcc-s1, libgl1, libegl1). NO qt6 / libtorrent here — if the build
          # weren't self-contained, the next step would find missing libraries.
          apt-get install -y ./nexa_*_amd64.deb

      - name: Assert nothing is missing
        run: |
          set -e
          fail=0
          check() {
            local missing
            missing=$(ldd "$1" 2>/dev/null | grep -i "not found" || true)
            if [ -n "$missing" ]; then
              echo "❌ $1"; echo "$missing"; fail=1
            else
              echo "✅ $1"
            fi
          }
          check /usr/lib/nexa/nexa
          check /usr/lib/nexa/nexa-host
          for p in $(find /usr/lib/nexa/plugins -name '*.so'); do check "$p"; done
          test -f /usr/lib/nexa/plugins/platforms/libqxcb.so \
            && echo "✅ xcb platform plugin present" \
            || { echo "❌ xcb platform plugin MISSING"; fail=1; }
          test -f /usr/lib/nexa/plugins/sqldrivers/libqsqlite.so \
            && echo "✅ sqlite driver present" \
            || { echo "❌ sqlite driver MISSING"; fail=1; }
          test -n "$(find /usr/lib/nexa/plugins/tls -name '*.so' 2>/dev/null)" \
            && echo "✅ TLS backend present (https works)" \
            || { echo "❌ TLS backend MISSING — every https download would fail"; fail=1; }
          # The bundled tools are checked the only way that means anything: by
          # RUNNING them. `check` above (and every ldd gate) passes for a binary
          # that exists and is executable, which is precisely what the broken
          # ffmpeg copy was — present, +x, and unable to start for want of
          # libavdevice. yt-dlp then merged nothing and YouTube downloads ended
          # as two files. A clean container is where that has to be caught.
          for tool in ffmpeg ffprobe yt-dlp; do
            check /usr/lib/nexa/$tool
            if /usr/lib/nexa/$tool -version >/dev/null 2>&1 \
               || /usr/lib/nexa/$tool --version >/dev/null 2>&1; then
              echo "✅ $tool runs"
            else
              echo "❌ $tool is present but CANNOT RUN — video merging is broken"; fail=1
            fi
          done
          if [ "$fail" -ne 0 ]; then
            echo "::error::.deb is NOT self-contained — see missing libs above"
            exit 1
          fi
          echo "🎉 .deb is fully self-contained"

  quality:
    name: Tests, audits, and extension packages
    runs-on: ubuntu-latest
    timeout-minutes: 30
    # The website's own checks — backend unit + integration tests against a
    # MariaDB service, frontend and admin lint/test/build/audit — live in
    # website.yml, which runs on every branch that touches ndm-website/, not
    # only main, and against production's engine rather than MySQL 8. This job
    # keeps the C++ tests, the browser e2e suite, static analysis and the
    # extension packages.
    steps:
      - uses: actions/checkout@v7
      - name: Install C++ dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y cmake ninja-build qt6-base-dev libqt6sql6-sqlite \
            libtorrent-rasterbar-dev nodejs npm zip
      - name: Build and test C++
        run: |
          cmake -B build-quality -G Ninja -DCMAKE_BUILD_TYPE=Debug -DNEXA_BUILD_TESTS=ON
          cmake --build build-quality
          ctest --test-dir build-quality --output-on-failure
      - name: Install frontend dependencies (Playwright + the jsdom the extension tests use)
        run: npm --prefix ndm-website/frontend ci

      - name: End-to-end (built site, stubbed API)
        working-directory: ndm-website/frontend
        run: |
          # Uses the runner's pre-installed Google Chrome (channel: chrome), so
          # no ~300 MB browser download. The suite stubs every API call, so it
          # needs neither the backend nor a database.
          npx playwright test --reporter=line

      - name: Upload e2e traces on failure
        if: failure()
        uses: actions/upload-artifact@v7
        with:
          name: playwright-traces
          path: ndm-website/frontend/test-results
          retention-days: 7

      - name: Static analysis (C++)
        run: |
          sudo apt-get install -y cppcheck
          cppcheck --version
          # Gates on the checks that find real defects (warning + portability).
          # "performance" is left OUT of the gate on purpose: its returnByReference
          # advice is wrong for Qt's implicitly-shared types (QString/QByteArray),
          # so it would fail the build for idiomatic code. Suppressions cover Qt
          # macros cppcheck cannot parse and local aggregate structs that are
          # brace-initialised at every use site.
          cppcheck --quiet --error-exitcode=1 \
            --enable=warning,portability \
            --inline-suppr --library=qt \
            --suppress=missingIncludeSystem \
            --suppress=missingInclude \
            --suppress=unusedFunction \
            --suppress=unknownMacro \
            --suppress=uninitMemberVarNoCtor \
            -DQT_VERSION=0x060500 '-DQT_VERSION_CHECK(a,b,c)=0x060400' \
            --std=c++17 -I src \
            src native-host
      - name: Package and validate extensions
        run: |
          ./extension-chromium/package.sh
          ./extension-firefox/build.sh
          unzip -t dist/nexa-chrome.zip
          unzip -t dist/nexa-firefox.zip
          node tests/ExtensionProviderConfigTest.js
          node tests/ExtensionSettingsTest.js
          # jsdom comes from the frontend's node_modules, installed by the
          # "Frontend and admin builds" step above. NEXA_REQUIRE_JSDOM makes a
          # missing dependency fail here rather than skip to a green tick.
          NEXA_REQUIRE_JSDOM=1 node tests/ExtensionContentTest.js
