node_modules
.env
dist
*.log
mongo-data

# Vite dependency pre-bundle cache (frontend/.vite, admin/.vite)
.vite/

# Uploaded release installers (large binaries — never committed)
backend/uploads/

# Throwaway MariaDB for the integration tests (test/tools/testdb.sh)
backend/.testdb/
